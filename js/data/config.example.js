/**
 * Copy to config.local.js (gitignored) and fill with your Supabase project credentials.
 * @example
 * export const DATA_CONFIG_LOCAL = {
 *   provider: "supabase",
 *   supabase: {
 *     url: "https://YOUR_PROJECT.supabase.co",
 *     anonKey: "YOUR_ANON_KEY",
 *   },
 * };
 */
export const DATA_CONFIG_LOCAL = {
  provider: "mock",
  supabase: {
    url: "",
    anonKey: "",
  },
};
